# CLAUDE.md

## 스페이싱/라운딩 값 규칙

이 규칙은 지금 있는 Figma 프레임 값을 tokens.css에 그대로 옮겨 넣는 작업에는 적용하지 않는다 (그 값들은 프레임에 있는 그대로 토큰화한다). 앞으로 프레임에 없는 새로운 스페이싱/라운딩 값이 필요해질 때만 참고한다.

1. 항상 짝수만 사용한다 — 소수점은 절대 금지. 단 `auto`/`fill`/`hug` 같은 레이아웃 키워드는 예외.
2. 8px 단위를 최우선으로 시도한다.
3. 8px으로 안 맞으면 4px 단위로 시도한다.
4. 그래도 안 맞으면 2px 단위로 시도한다.
5. 정말 애매한 경우에만 최후 수단으로 1px을 허용한다.

