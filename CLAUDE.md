# CLAUDE.md

## 1440 브레이크포인트 규칙

1920 기준으로 만들어진 화면/컴포넌트에 1440px 이하 대응을 추가할 때 아래 패턴을 기본으로 따른다. 여기 없는 새로운 케이스가 나오면 임의로 결정하지 말고 먼저 사용자에게 물어본다.

- **그리드 마진**: 1920은 120px을 그대로 쓴다 (12컬럼 그리드 통일을 위한 의도적 예외이며, Figma 실측값인 210px이 아니다). 1440은 Figma 실측값을 그대로 따른다 (현재 170px). 즉 "실측값 대신 예외를 적용하는" 경우는 1920 한 쪽뿐이고, 1440은 항상 실측값 기준이다.

- **타이포그래피**: 브레이크포인트별로 사이즈가 다른 두 버전이 필요하면 토큰 이름에 접미사를 붙여 구분한다 (예: `--font-navigation-l-*` / `--font-navigation-s-*`). 기존 토큰 값 자체를 바꾸는 게 아니라 이름을 나누는 방식이므로, 값이 실제로 달라지면 그 토큰을 참조하던 모든 곳까지 전부 리네이밍이 필요하다는 점을 잊지 않는다. 반대로 참조하는 곳이 하나뿐이거나 사이즈 사다리(Body-S/M/L 등)가 이미 있는 경우엔 새 접미사 토큰을 만들지 않고 기존 사이즈 단계를 그대로 재사용한다.

- **컴포넌트 폭/크기 확대·축소** (예: 검색창을 1.2배로 키운 경우): 확대는 1920에만 적용하고, 1440은 확대 전 원본 값으로 되돌린다. 즉 1920이 "커스텀 예외(확대된 값)"이고 1440이 오히려 "기본값(원본 값)"인 경우가 있을 수 있다 — 무조건 1920이 기본, 1440이 예외라고 가정하지 않는다.

- **Header/Footer**: 다른 화면 프레임 안에 같이 그려진 Header/Footer 형태를 기준으로 삼지 않는다. 항상 Header/Footer 전용으로 제공되는 1920+1440 프레임을 최종 기준으로 삼아 처음부터 다시 실측한다.

- **새 컴포넌트/구조**: 1440 대응을 위해 새 컴포넌트, 새 훅, 새 구조적 메커니즘(예: JS 브레이크포인트 훅)이 필요할 것 같으면 임의로 만들지 않고 먼저 제안하고 확인받는다.

### 새 1920 디자인을 받았을 때

앞으로 새로운 1920 디자인이 오면, 위 패턴들을 기본으로 적용해서 1440 대응을 진행한다. 다만 위 규칙으로 커버되지 않는 새로운 유형의 케이스가 나타나면, 스스로 판단해서 결정하지 말고 반드시 먼저 사용자에게 물어보고 진행한다.
