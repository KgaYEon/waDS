# CLAUDE.md

## 스페이싱/라운딩 값 규칙

이 규칙은 지금 있는 Figma 프레임 값을 tokens.css에 그대로 옮겨 넣는 작업에는 적용하지 않는다 (그 값들은 프레임에 있는 그대로 토큰화한다). 앞으로 프레임에 없는 새로운 스페이싱/라운딩 값이 필요해질 때만 참고한다.

1. 항상 짝수만 사용한다 — 소수점은 절대 금지. 단 `auto`/`fill`/`hug` 같은 레이아웃 키워드는 예외.
2. 8px 단위를 최우선으로 시도한다.
3. 8px으로 안 맞으면 4px 단위로 시도한다.
4. 그래도 안 맞으면 2px 단위로 시도한다.
5. 정말 애매한 경우에만 최후 수단으로 1px을 허용한다.

## 반응형 사이즈 조정 규칙 (1920→1440)

1920 프레임 기준으로 만들고 1440에서 줄여야 하는 값(폰트 크기 포함 모든 사이즈)은 **rem + `clamp()` 기반 로컬 스케일 변수**를 기본 방식으로 쓴다:

```css
--page-scale: clamp(0.75, calc(0.75 + (100vw - 1440px) / (1920px - 1440px) * 0.25), 1);
```

1920에서 100%, 1440 이하에서 75%로 자연스럽게 줄어들고 그 사이는 뷰포트 폭에 비례해 부드럽게 보간된다.

**`--page-scale`는 공용 변수명이다.** 화면(스크린) 레벨 컴포넌트는 전부 이 이름을 자기 최상위 클래스에 로컬 스코프로 정의한다 (Home의 `--page-scale`은 `.page`에, GameGroupScreen의 `--page-scale`은 `.screen`에 — 그 하위에 속하지 않는 Header/Footer 같은 컴포넌트로 새지 않게). 이렇게 이름을 통일해두는 이유: `GameCard` 같은 여러 화면에서 재사용되는 공용 컴포넌트가 `calc(var(--font-x-size) * var(--page-scale, 1))`처럼 **fallback 값 1과 함께** 이 변수를 참조하면, `--page-scale`을 정의한 화면(Home, GameGroupScreen 등) 안에서는 자동으로 같이 줄어들고, 정의하지 않은 다른 화면(Filter, SearchResults 등)에서는 fallback 1이 적용돼 기존 고정 크기 그대로 유지된다 — 화면마다 변수 이름을 따로 지으면 이 fallback 트릭이 안 통한다.

예외: `Header`는 화면이 아니라 모든 화면에 공통으로 얹히는 컴포넌트라서, 화면들과 이름이 겹치면 안 되므로 자기 것만의 이름 `--header-scale`을 쓴다 (`Header.module.css`, `.header`에 스코프).

**이전에 쓰던 "타이포 토큰 -l/-s 접미사를 media query로 통째로 교체" 방식(예: `NavTab`/`BoardNav`의 `--font-navigation-l-*` → `--font-navigation-s-*`, `max-width: 1440px`에서 전환)은 더 이상 새 작업의 기본 방식이 아니다.** 이미 그 방식으로 구현된 기존 컴포넌트는 그대로 두지만, 새로 반응형 사이즈가 필요한 곳에는 쓰지 않는다. 두 방식을 같은 값에 동시에 적용하면 안 된다 — clamp로 계속 줄어들고 있는 값이 특정 breakpoint에서 갑자기 다른 고정 토큰으로 점프해버려서 서로 충돌한다 (폰트가 안 줄어들거나 이중으로 줄어드는 버그가 생김).

**How to apply:** 1920 프레임을 기준으로 만들고 1440까지 줄여야 하는 새 화면에는 항상 자기 최상위 클래스에 `--page-scale` 로컬 스케일 변수를 정의한다. 이미 존재하는 shared 컴포넌트(Button/Chip 등)의 내부 폰트 크기는 기본적으로 이 스케일 변수의 영향을 받지 않는다 — 여러 화면에서 재사용되므로 특정 화면의 스케일 변수에 종속시키지 않고, 화면 쪽에서 그 컴포넌트가 차지하는 크기(너비 등)만 스케일하는 게 기본이다. 다만 `GameCard`처럼 자기 안쪽 캡션 패딩/폰트가 카드 자체 크기(컨테이너 폭)에 비례해 같이 줄어들지 않으면 잘리는 것처럼 시각적으로 깨지는 경우엔, `calc(var(--font-x) * var(--page-scale, 1))`처럼 fallback 1을 준 채로 `--page-scale`을 참조해도 된다 — `--page-scale`을 정의하지 않은 화면(Filter, SearchResults 등)에서는 fallback이 적용돼 기존과 동일하게 유지된다.

